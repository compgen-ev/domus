{
  "name": "house-history",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && mkdir -p dist && cp landing/index.html dist/index.html",
    "preview": "vite preview --outDir dist"
  },
  "dependencies": {
    "lit": "^3.2.0",
    "maplibre-gl": "^4.7.1"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
